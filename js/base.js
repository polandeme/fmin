// var Cell = {
// 	i,
// 	j,
// 	value,
// 	available
// }

function initCell(n) {
	var list = '';
	var li = '';
	for(var i = 0; i < n; i++) {
		li = '<li class="cell-item">' + '</li>';
		list += li;
	}
}
/**
 * 1. 多个对象push进去
 * 2. 增加attr, 点击后改变当前attr,获取新的attr.
 * 3. 
 */

 /*
  * 函数名驼峰
  * 变量名 下划线
  * 
  */